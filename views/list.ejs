<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>To Do List</title>
</head>
<body>

 <% if (kindOfDay === "Saturday") { %>
    <h1 style="color: #f3bdc6"><%= kindOfDay %> ToDo List</h1>
 <% } else { %>
    <h1 style="color: #5252ca"><%= kindOfDay %> ToDo List</h1>
 <% } %>
   
</body> 
</html>